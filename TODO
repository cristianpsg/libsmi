
o check all `TODO' marks in parser.y for open questions.
o add syntax error output (yyerror()) to parser.y.
o handle forbidden WS at some places ( - number, Module . label, ...)
o detect IMPORTs of SMIv1 specific and SMIv2 specific items in the
  same module, e.g. old OBJECT-TYPE macro and (new) NOTIFICATION-TYPE macro.
o ensure order without forward references in typedef sming dump.
o dump-sming.c: all(!) kinds of index clauses
o /usr/local/lib/tnm3.0.0/mibs/RFC1382-MIB:1609:
  OBJECT IDENTIFIER DEFVALs must be expressed as a single identifier
                  DEFVAL { {0 0}...
o have seen cases like `default ;'. where? 
o BIN and HEX DEFVALs in SMIv1/v2 Parser.

o READ_CREATE should *never* occur in data.c / smi.c
o simple documented example

o redesign of data structures: make SmiXXX structures the heads of the
  according data.h structures. This would obsolete the Free() functions
  and enable fast getNext() functions by accessing the underlying lists.

o Web online conversion to SMIng?

o smiGetNames()

o thread safety (global vars? static vars? strtok() and other funcs?)
  different views

o SmiDecl is language dependant.

o add much more checks to the smiv1/v2 and sming parsers
o smi_errno ?!
o set up a catalogue of errors detected by the smiv1/v2 parser?
o grouping of error and warning messages
o   does table node have exactly one row sub node with oid == 1? (2578.7.10)
o   non continious sub oids in column node list?
o   warning on NotificationName/ObjectName/ExtUTCTime definitions.

o if `parse error' occurs, the database is left in an incosistent state,
  e.g. index labels of sming modules are not adjusted.

o eliminate length restrictions (grep MAX_ *.[chly])

o `::' instead of `.' as separator in tools' output? (Juergen)

o tcpdump (Juergen)

o need handle to distinguish different views.

o FreeBSD/BSD/OS: quad_t

o clearly separate language dependant information at the API:
  SMI_DECL*, SMI_STATUS_*, SMI_ACCESS_*

o need a more efficient OID representation: length&int[] instead of char *

o DISPLAY_HINT interpreter function

o Java OID/Name Lookup Service

o eliminate MAX_DESCRIPTOR

o data.c:init()
